<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.RecipesApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.RecipesApp" parent="Base.Theme.RecipesApp" />
    <!--DETAILS-->
    <style name="Details" parent="Base.Theme.RecipesApp">
        <item name="android:layout_margin">8dp</item>
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:drawablePadding">16dp</item>
        <item name="android:textStyle">italic</item>
    </style>
    <style name="DetailsBox" parent="Base.Theme.RecipesApp">
        <item name="background">@color/details_bg</item>
    </style>
    <!--TEXT-->
    <style name="NormalText" parent="Base.Theme.RecipesApp">
        <item name="android:layout_margin">16dp</item>
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">@color/black</item>
    </style>
    <style name="Author" parent="NormalText">
        <item name="android:textColor">@color/magenta</item>
        <item name="android:textStyle">bold</item>
    </style>

</resources>